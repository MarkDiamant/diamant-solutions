'use client';
import {useState} from 'react';
export default function CheckoutButton({service,plan,users,ai,interval,children,className}){const [busy,setBusy]=useState(false),[error,setError]=useState('');async function go(e){e.preventDefault();if(busy)return;setBusy(true);setError('');try{const r=await fetch('/api/checkout',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({service,plan,users,ai,interval})});const j=await r.json();if(!r.ok||!j.url)throw new Error(j.error||'Checkout could not be started.');window.location.href=j.url}catch(err){setError(err.message);setBusy(false)}}return <span className={className||''}><button type="button" onClick={go} disabled={busy}>{busy?'Opening secure checkout…':children}</button>{error&&<small style={{display:'block',marginTop:6}}>{error}</small>}</span>}