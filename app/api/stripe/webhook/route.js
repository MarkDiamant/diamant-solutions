import {NextResponse} from 'next/server';
import crypto from 'crypto';
const SUPABASE_URL=process.env.NEXT_PUBLIC_SUPABASE_URL;
const SERVICE_KEY=process.env.SUPABASE_SERVICE_ROLE_KEY;
const WEBHOOK_SECRET=process.env.STRIPE_WEBHOOK_SECRET;
function verify(raw,header){if(!WEBHOOK_SECRET||!header)return false;const parts=Object.fromEntries(header.split(',').map(x=>x.split('=')));const t=parts.t,v1=header.split(',').find(x=>x.startsWith('v1='))?.slice(3);if(!t||!v1||Math.abs(Date.now()/1000-Number(t))>300)return false;const expected=crypto.createHmac('sha256',WEBHOOK_SECRET).update(t+'.'+raw).digest('hex');try{return crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(v1))}catch{return false}}
async function upsertSession(s){if(!SUPABASE_URL||!SERVICE_KEY)throw new Error('Supabase server configuration missing');const m=s.metadata||{},service=m.service_type==='website'?'website':'business_software',plan=m.plan_key||(service==='website'?'monthly':'business_software'),paid=s.payment_status==='paid'||(s.mode==='subscription'&&s.status==='complete'&&s.payment_status==='no_payment_required');const row={service_type:service,plan_key:plan,users:service==='business_software'?Math.max(2,Number(m.users)||2):null,ai_included:m.ai_included==='true',billing_interval:m.billing_interval||null,stripe_checkout_session_id:s.id,stripe_customer_id:typeof s.customer==='string'?s.customer:null,stripe_subscription_id:typeof s.subscription==='string'?s.subscription:null,customer_email:s.customer_details?.email||s.customer_email||null,payment_status:paid?'paid':'pending',paid_at:paid?new Date().toISOString():null};const r=await fetch(SUPABASE_URL+'/rest/v1/checkout_orders?on_conflict=stripe_checkout_session_id',{method:'POST',headers:{apikey:SERVICE_KEY,Authorization:'Bearer '+SERVICE_KEY,'Content-Type':'application/json',Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify(row)});if(!r.ok)throw new Error('Supabase fulfilment failed: '+await r.text())}
export async function POST(req){const raw=await req.text();if(!verify(raw,req.headers.get('stripe-signature')))return new NextResponse('Invalid signature',{status:400});try{const event=JSON.parse(raw);if(event.type==='checkout.session.completed'||event.type==='checkout.session.async_payment_succeeded')await upsertSession(event.data.object);return NextResponse.json({received:true})}catch(e){console.error('stripe webhook',e);return new NextResponse('Webhook processing failed',{status:500})}}